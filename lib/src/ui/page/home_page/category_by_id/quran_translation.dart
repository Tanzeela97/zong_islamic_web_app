import 'package:flutter/material.dart';


class QuranAndTranslation extends StatelessWidget {
  const QuranAndTranslation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Quran and Translation'),
    );
  }
}
